<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_nav_graph"
    app:startDestination="@id/startFragment">

    <fragment
        android:id="@+id/flowFragment"
        android:name="com.example.cryptocurrencytest.views.FlowFragment"
        android:label="fragment_flow"
        tools:layout="@layout/fragment_flow" >
        <action
            android:id="@+id/action_flowFragment_to_startFragment"
            app:destination="@id/startFragment" />
    </fragment>
    <fragment
        android:id="@+id/startFragment"
        android:name="com.example.cryptocurrencytest.views.StartFragment"
        android:label="fragment_start"
        tools:layout="@layout/fragment_start" >
        <action
            android:id="@+id/action_startFragment_to_flowFragment"
            app:destination="@id/flowFragment" />
    </fragment>
</navigation>